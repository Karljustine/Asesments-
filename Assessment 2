#include #include #include <math.h> using namespace std;

int main() { string name; string number;

cout << " Please do enter your name \n";
cin >> name;

int factorial(int);
int fact, value;
cout << "Enter any number: ";
cin >> value;
while (cin.fail())
{cin.clear();
cin.ignore(1000, '\n');
    cin >> value;
}

    fact = factorial(value);
    cout << "Factorial of a number is: " << fact << endl;
    return 0;
}
int factorial(int n)

{ cout << " square=" << pow(n, 2) << " cube= " << pow(n, 3) << endl;

for (int num = 20; num <= 30; num++)

{
    cout << n << " * " << num << " is " << n * num;
}
{
    if (n < 0)
        return(-1);
    if (n == 0)
        return(1);
    else
    {
        return(n * factorial(n - 1));

    }
    for (int num = 20; num <= 30; num++)

    {
        cout << n << " * " << num << " is " << n * num;
    }
}
}
